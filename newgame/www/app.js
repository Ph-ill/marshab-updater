const S={state:null,tab:'hab',content:{},diag:null,lastMsg:'',fetchedAt:0,logSeeded:false,logs:[],unreadLogs:0,tutStep:0,tutDone:false,tutTarget:'',resOpen:false,settingsOpen:false,resetArmed:false,tutReview:false,actOpen:false,stageSeen:null,stageFlash:false,surfaceTutDone:false,tradeTutDone:false,tutScrollY:null,logFlashUntil:0,audioCtx:null,audioReady:false};
const $=q=>document.querySelector(q);
const TYPES=['actions','beats','archive','events','away','ui','ending','letters'];
const ICON={o2:'🫁',power:'⚡',water:'💧',food:'🥫',regolith:'🪨',population:'👥',atmosphere:'🌫️',temperature:'🌡️',biomass:'🌱',ice:'🧊',alloy:'🔩',polymer:'🧪',rare_metals:'💎',vent_co2:'🌬️',reroute_power:'⚡',collect_ice:'🧊',sift_regolith:'🪨',grow_rations:'🌱',decode_signal:'📡',survey_crater:'🛞',print_spares:'🛠️',stabilize_dome:'🏕️',run_atmo_processor:'🌫️',seed_bioreactor:'🧫',open_airlock:'🚪'};
const FALLBACK={ui_tab_hab:'Hab',ui_tab_surface:'Surface',ui_tab_colony:'Colony',ui_tab_trade:'Trade',ui_tab_archive:'Archive',ui_tab_ending:'End',ui_header_actions:'Operations',ui_header_archive:'Archive',ui_header_letters:'Letters',ui_header_ending:'Finale',ui_button_copy_code:'Copy Code',ui_button_redeem_code:'Redeem Code',ui_button_tend:'Tend',ui_resource_o2:'O₂',ui_resource_power:'Power',ui_resource_water:'Water',ui_resource_food:'Rations',ui_resource_regolith:'Regolith',ui_resource_population:'Crew',ui_resource_atmosphere:'Atmosphere',ui_resource_temperature:'Temp',ui_resource_biomass:'Biomass',ui_resource_alloy:'Alloy',ui_resource_polymer:'Polymer',ui_resource_rare_metals:'Rare Metals',ui_resource_ice:'Ice'};
const ACTION_FALLBACK={vent_co2:{label:'Vent CO₂',flavor:'Bleed stale air through the scrubbers.'},reroute_power:{label:'Reroute Power',flavor:'Balance batteries and sun-starved circuits.'},collect_ice:{label:'Collect Ice',flavor:'Send drones to scrape usable ice.'},sift_regolith:{label:'Sift Regolith',flavor:'Sort dust and rock into feedstock.'},grow_rations:{label:'Grow Rations',flavor:'Tend the trays until dinner looks possible.'},decode_signal:{label:'Decode Signal',flavor:'Pull another pattern from the buried transmission.'},survey_crater:{label:'Survey Crater',flavor:'Roll beyond the lights and map useful ground.'},print_spares:{label:'Print Spares',flavor:'Turn polymer and alloy into emergency parts.'},stabilize_dome:{label:'Stabilize Dome',flavor:'Seal seams and settle the pressure envelope.'},run_atmo_processor:{label:'Run Atmo Processor',flavor:'Push another breath into the Martian sky.'},seed_bioreactor:{label:'Seed Bioreactor',flavor:'Wake the microbes and feed the slow green engine.'},open_airlock:{label:'Open Airlock',flavor:'Step into the changed world.'}};
function ding(){try{let C=window.AudioContext||window.webkitAudioContext;if(!C)return;let ctx=S.audioCtx||(S.audioCtx=new C());if(ctx.state==='suspended')ctx.resume();let t=ctx.currentTime,o=ctx.createOscillator(),g=ctx.createGain();o.type='sine';o.frequency.setValueAtTime(880,t);o.frequency.exponentialRampToValueAtTime(1320,t+.08);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(.08,t+.015);g.gain.exponentialRampToValueAtTime(.0001,t+.22);o.connect(g);g.connect(ctx.destination);o.start(t);o.stop(t+.24)}catch(e){}}
function log(m,silent=false){if(!m)return;S.logs.unshift({text:m,t:Date.now()});S.logs=S.logs.slice(0,80);if(!silent&&S.tab!=='console'){S.unreadLogs=Math.min(99,S.unreadLogs+1);S.logFlashUntil=Date.now()+1800;ding();setTimeout(()=>{if(S.logFlashUntil&&Date.now()>=S.logFlashUntil){S.logFlashUntil=0;render()}},1850)}}
function esc(s){return String(s||'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
function logFmt(s){return esc(s).replace(/\*([^*\n]{1,80})\*/g,'<b>$1</b>').replace(/\n/g,'<br>')}
async function api(p,o={}){let r=await fetch(p,{method:o.body?'POST':(o.method||'GET'),headers:{'Content-Type':'application/json'},body:o.body&&JSON.stringify(o.body)});return r.json()}
function carried(){try{return JSON.parse(localStorage.getItem('marsHabPackages')||'[]')}catch(e){return []}}
function loadTut(){try{S.tutDone=localStorage.getItem('marsHabTutorialDone')==='1';S.surfaceTutDone=localStorage.getItem('marsHabSurfaceTutorialDone')==='1';S.tradeTutDone=localStorage.getItem('marsHabTradeTutorialDone')==='1';S.tutStep=Math.max(0,Math.min(9,+(localStorage.getItem('marsHabTutorialStep')||'0')));if(!S.tutDone){if(S.tutStep===4||S.tutStep===5)S.tab='console'}}catch(e){}}
function tutDone(){return S.tutDone}
function tutStep(){return S.tutStep||0}
function setTutStep(n){S.tutStep=Math.max(0,Math.min(9,n));if(S.tutStep===4||S.tutStep===5)S.tab='console';else S.tab='hab';try{localStorage.setItem('marsHabTutorialStep',String(S.tutStep))}catch(e){}}
function closeTut(done){if(done){S.tutDone=true;try{localStorage.setItem('marsHabTutorialDone','1');localStorage.setItem('marsHabTutorialSeen','1')}catch(e){}}S.tutReview=false;render()}
function openTut(){S.tutDone=false;S.tutReview=localStorage.getItem('marsHabTutorialSeen')==='1';setTutStep(0);try{localStorage.removeItem('marsHabTutorialDone')}catch(e){}render()}
function backTut(){S.tutDone=false;setTutStep(Math.max(0,tutStep()-1));try{localStorage.removeItem('marsHabTutorialDone')}catch(e){}render()}
function starterLoopBuilt(u){return ['solar_array','ice_well','greenhouse'].every(k=>u&&u[k]&&u[k].owned)}
function nextStarterNeed(u){return ['solar_array','ice_well','greenhouse'].find(k=>!(u&&u[k]&&u[k].owned))||''}
function starterName(k){return k==='solar_array'?'Solar Array':k==='ice_well'?'Ice Well':k==='greenhouse'?'Greenhouse':'starter loop'}
function starterTarget(k){let m=S.state?.upgrades?.[k],r=S.state?.resources||{};if(!m)return '[data-guide=mods]';let c=m.cost||{},short=Object.keys(c).find(x=>(r[x]||0)<c[x]);if(!short&&m.buildable!==false)return `[data-build=${k}]`;let act={regolith:'sift_regolith',power:'reroute_power',water:'collect_ice',ice:'collect_ice',food:'grow_rations'}[short];return act?`[data-act=${act}]`:`[data-build=${k}]`}
function starterNeedHint(k){let m=S.state?.upgrades?.[k],r=S.state?.resources||{};if(!m)return '';let c=m.cost||{},short=Object.keys(c).filter(x=>(r[x]||0)<c[x]).map(x=>`${nice(x)} ${fmt(r[x]||0)}/${fmt(c[x])}`);if(!short.length&&m.buildable!==false)return '';return short.length?`<p class=muted>Need for ${starterName(k)}: ${short.join(' · ')}. Use the highlighted operation, then build ${starterName(k)}.</p>`:''}
function tutBody(b){if(Array.isArray(b))return b.map(x=>Array.isArray(x)?`<ul>${x.map(i=>`<li>${logFmt(i)}</li>`).join('')}</ul>`:`<p>${logFmt(x)}</p>`).join('');return `<p>${logFmt(b)}</p>`}
function catchUpTut(){if(tutDone()||!S.state)return;let u=S.state.upgrades||{},step=tutStep(),next=step;if(step===6&&u.solar_array&&u.solar_array.owned)next=starterLoopBuilt(u)?8:7;if(step===7&&starterLoopBuilt(u))next=8;if(next!==step)setTutStep(next)}
function tutorial(){if(tutDone()||!S.state)return '';if(!S.tutReview)catchUpTut();let s=S.state,u=s.upgrades||{},step=tutStep();let steps=[
 {t:'Welcome to MarsHab',b:['Your MarsHab device is hosting this whole colony console. You are the *quiet automation layer* between six tired settlers and a very large, very cold planet.','The hab is alive, but only just: air leaks into scrubbers, batteries sag, water is rationed, and every useful thing begins as dust, ice, and sunlight.'],a:'Continue',target:''},
 {t:'Your first job',b:['MarsHab is an idle survival game about turning *fragile systems* into *dependable loops*.','You will tap active operations for quick help, build modules for lasting production, and check back as the colony slowly becomes less emergency-shaped.','*Pressure* is measured in mb, short for millibars: it tells you how much atmosphere Mars has outside the hab. *Confidence* is the crew’s safety estimate, combining pressure, temperature, and biology into one cautious ‘are we nearly breathable?’ number. *Legacy* is only for later replays: each completed run leaves a tiny boost behind.','If anything feels mysterious, the Guide button brings this help back.'],a:'Show me the console',target:''},
 {t:'Read the resource strip',b:['The top strip is *mission control in miniature*. The six always-visible chips are life support: O₂, power, water, rations, regolith, and crew.','Tap any chip now to reveal its name. Later, More opens the full engineering readout with rates and advanced materials.','Act and system details live behind the small 🚀 Act button. You can ignore them while learning; they are there when you want the engineering readout. The Surface tab is also safe to ignore for now; when expeditions and terraforming controls become relevant, MarsHab will explain them in place.'],a:'Waiting for resource tap…',target:'#res',wait:true},
 {t:'Operations are your hands',b:'*Operations* are quick orders you can send right now. They *buy time*: vent stale air, reroute power, collect ice, sift dust. Use one ready operation now. You are not solving Mars forever yet — you are making the next few minutes safer.',a:'Waiting for operation…',target:'[data-act]',wait:true},
 {t:'The log tells the story',b:['Every operation, module, away report, and strange signal writes into the *Console Log*. It is not just a receipt list — it is how MarsHab tells you what the crew noticed and what the planet is starting to answer.','The operation you just ran added a new line. Tap Log now and look at the newest entry at the bottom of the terminal. When the Log button glows or shows a • number, there is something new to read.'],a:'Waiting for Log tab…',target:'nav',wait:true},
 {t:'Read the newest entry',b:['This terminal is the *story feed*. The newest line is always at the bottom and marked *LATEST*.','As you build, leave, return, trade, or uncover odd signals, this is where MarsHab speaks back. Check it whenever the Log button says something new is waiting.'],a:'Back to building',target:''},
 {t:'Modules make habits',b:'Operations are *emergency nudges*. Modules are *habits* the colony can keep doing without you. Solar Array is first because power makes every other plan less desperate: pumps run, lamps glow, and the crew stops choosing which system gets electricity.',a:'Waiting for Solar Array…',target:'[data-build=solar_array]',wait:true},
 {t:'The first loop',b:'Good. One panel is not a city, but it is a start. Finish the starter loop now: *Solar Array* feeds *Ice Well*, Ice Well feeds *Greenhouse*, Greenhouse feeds rations and oxygen. Build the highlighted module so the hab has its first real foothold on Mars.',a:'Waiting for starter loop…',target:'[data-guide=mods]',wait:true},
 {t:'Check in, then let it breathe',b:'MarsHab *keeps working while powered*. You do not need to stare at it every second; return later to read logs, spend gains, and shore up whichever system is grumbling loudest. The colony should feel like a living little machine, not a spreadsheet demanding tribute.',a:'Continue',target:''},
 {t:'You have the console',b:['That is the shape of MarsHab:', ['Watch the resource strip for pressure points.','Use operations when something is tight.','Build modules when you can make a loop permanent.','Read the Log when MarsHab says something new is waiting.'], '*Trade is optional* social machinery. It stays out of this first lesson; when you open Trade, MarsHab will explain care packages, redeeming codes, and passport visits there.', 'Next, keep strengthening the colony: add more solar, water, and greenhouse capacity, then follow the new modules and archive entries as they unlock.', 'The logs are already hinting that this site is *not as empty as it looks*. Some readings in the dust are too orderly. Some old signals know too much.', 'Keep the lights on, keep the crew breathing, and see what *Mars remembers*.'],a:'Enter the hab',target:''}];
 let x={...steps[step]};let hint='',review=S.tutReview;
 if(step===7&&!review){let need=nextStarterNeed(u);if(need){x.target=starterTarget(need);x.a=`Waiting for ${starterName(need)}…`;hint+=starterNeedHint(need)}}
 if(review&&x.wait){x.wait=false;x.a='Continue'}
 S.tutTarget=x.target||'';
 if(!review&&(step===6||step===7)){let built=['solar_array','ice_well','greenhouse'].filter(k=>(u[k]&&u[k].owned));hint+=`<p class=muted>Starter loop progress: ${built.length}/3 built.</p>`}
 if(review)hint+=`<p class=muted>Guide review mode: no actions or builds required.</p>`;
 let primary=x.wait?`<p class=wait>▶ ${x.a}</p>`:`<button data-tut=next>${x.a}</button>`;
 return `<div id=tutorial class=tut${step}><div class=tutShade></div><div class=tutCard><h2>🧭 ${x.t}</h2>${tutBody(x.b)}${hint}${x.wait?'<p class=need>Action required: use the highlighted part of the interface to continue.</p>':''}<div class=tutBtns><button data-tut=skip>${review||step===9?'Close':'Skip'}</button>${step?'<button data-tut=back>Back</button>':''}${primary}</div></div></div>`}



function surfacePrompt(){if(!tutDone()||S.surfaceTutDone||S.tab!=='hab'||!surfaceOps().length)return '';S.tutTarget='[data-tab=surface]';return `<div id=tutorial class=surfacePrompt><div class=tutShade></div><div class=tutCard><h2>🧭 SURFACE IS READY</h2>${tutBody([
'The colony can now work beyond the first hab loop. Surface expeditions and terraforming controls are ready to explain themselves.',
'Tap *Surface* now. Once you arrive there, MarsHab will walk you through what those outside-hab operations do and when to use them.'
])}<p class=wait>▶ Waiting for Surface tab…</p></div></div>`}
function tradeTutorial(){if(!tutDone()||S.tradeTutDone||S.tab!=='comms')return '';S.tutTarget='';return `<div id=tutorial class=tradeTut><div class=tutShade></div><div class=tutCard><h2>🧭 TRADE & PASSPORTS</h2>${tutBody([
'Trade is optional. You can finish MarsHab alone, but shared codes let habs send small boosts and passport visits.',
['Copy Code makes a care-package code from surplus. Share the text; another MarsHab device can redeem it once.','Redeem Code accepts a code you were given, adds the package here, and records the visit.','Tend is a friendly check-in: it stamps a passport visit and gives that hab a small production nudge.','Passport visits slowly raise the production bonus shown on this page. Helpful, never required.'],
'Codes are plain text so they work offline, over chat, or across devices without a central server.'
])}<div class=tutBtns><button data-trade-tut=close>Got it</button></div></div></div>`}
function surfaceOps(){return ['survey_crater','run_atmo_processor','seed_bioreactor','stabilize_dome'].filter(a=>(S.state?.actions||[]).includes(a))}
function surfaceTutorial(){if(!tutDone()||S.surfaceTutDone||S.tab!=='surface'||!surfaceOps().length)return '';S.tutTarget='';return `<div id=tutorial class=surfaceTut placeBottom><div class=tutShade></div><div class=tutCard><h2>🧭 SURFACE OPERATIONS</h2>${tutBody(['This tab has woken up because the colony can finally work *outside* the first hab loop.',['Surface operations are larger, slower orders: scouting crater routes, stabilizing dome systems, running atmosphere machinery, or seeding biology.','They usually cost more power/materials than hab operations, but they push *exploration*, *terraforming*, and the story forward.','Use them when the current Act asks for work beyond the hab. If buttons are missing or greyed out, build the matching modules first.'],'Surface work is where Mars stops being only a survival problem and starts becoming a planet you can change.'])}<div class=tutBtns><button data-surface-tut=close>Got it</button></div></div></div>`}
function maybeTut(kind,data){
 if(tutDone())return false;
 let step=tutStep();
 if((step===2&&kind==='resource')||(step===3&&kind==='action')||(step===4&&kind==='tab'&&data==='console')||(step===6&&kind==='build'&&data==='solar_array')){setTutStep(step+1);return true}
 if(step===7&&kind==='build'){let u=S.state?.upgrades||{};if(starterLoopBuilt(u)){setTutStep(8);return true}}
  return false;
}

function saveCarried(a){try{localStorage.setItem('marsHabPackages',JSON.stringify(a.slice(-12)))}catch(e){}}
function rememberCode(c){let a=carried();if(c&&!a.includes(c)){a.push(c);saveCarried(a)}}
function forgetCode(c){saveCarried(carried().filter(x=>x!==c))}
async function loadContent(t){if(!S.content[t]){try{S.content[t]=await api('/api/content/'+t)}catch(e){S.content[t]={}}}return S.content[t]}
function val(v,field){if(!v)return '';if(typeof v==='string')return v;if(field&&v[field])return v[field];if(v.text)return v.text;if(v.body&&v.title)return v.title+'\n\n'+v.body;if(v.body)return v.body;if(v.title)return v.title;if(v.label)return v.label;return JSON.stringify(v)}
function txt(t,id,field){let d=S.content[t]||{},key=id,result=null;if(t==='actions'){if(id.includes(':')){let p=id.split(':');key=p[0];result=p[1]}else if(!id.startsWith('act_'))key='act_'+id}let v=d[key];if(!v&&t==='ui')return FALLBACK[key]||FALLBACK[id]||id.replace(/^ui_(tab|resource|header|button)_/,'').replace(/_/g,' ');if(!v&&t==='actions'){let bare=key.replace(/^act_/,'');let f=ACTION_FALLBACK[bare]||{};return field==='flavor'?(f.flavor||'System ready.'):field==='label'?(f.label||bare.replace(/_/g,' ')):f.label||bare.replace(/_/g,' ')}if(!v)return '[STUB '+key+(result?':'+result:'')+']';return val(v,field||result)}
function icon(k){return ICON[k]||ICON[k.replace(/^act_/,'')]||'◈'}
function line(id){if(!id)return '';if(id.startsWith('act_'))return txt('actions',id);if(id.startsWith('beat_'))return txt('beats',id);if(id.startsWith('evt_'))return txt('events',id);if(id.startsWith('away_'))return txt('away',id);if(id.startsWith('end_'))return txt('ending',id);return id}
function fmt(n){if(n===undefined||n===null)return '0';if(Math.abs(n)>=100)return Math.round(n).toString();if(Math.abs(n)>=10)return (Math.round(n*10)/10).toString();return (Math.round(n*100)/100).toString()}
function pct(n){return fmt((n||0)*100)+'%'}
function nice(k){return txt('ui','ui_resource_'+k)||k.replace(/_/g,' ')}
function cost(c){return Object.keys(c||{}).map(k=>icon(k)+' '+nice(k)+' '+fmt(c[k])).join(' · ')||'free'}
function rate(v,k){if(!v)return '';let per=v*86400;if(k==='atmosphere'||k==='temperature'||k==='biomass')return (per>=0?'+':'')+fmt(per*100)+'/sol';return (per>=0?'+':'')+fmt(per)+'/sol'}
function res(){let s=S.state,r=s.resources||{},rates=s.rates||{},caps=s.caps||{};
 $('#res').className=S.resOpen?'open':'';
 let primary=['o2','power','water','food','regolith','population'];
 let secondary=['atmosphere','temperature','biomass','ice','alloy','polymer','rare_metals'];
 let relevant=k=>r[k]!==undefined&&((r[k]||0)>0||(rates[k]||0)!==0||['atmosphere','temperature','biomass'].includes(k)&&((s.act||1)>=3));
 let shown=primary.filter(k=>r[k]!==undefined), hidden=secondary.filter(relevant);
 let chip=k=>`<div class=r data-res="${k}" data-label="${nice(k)}"><b>${icon(k)} <span>${nice(k)}</span></b><strong>${k==='atmosphere'||k==='temperature'||k==='biomass'?pct(r[k]):fmt(r[k])}${S.resOpen&&caps[k]&&caps[k]<999999?' / '+fmt(caps[k]):''}</strong><em>${rate(rates[k],k)}</em></div>`;
 $('#res').innerHTML=`<div class=resTop>${shown.map(chip).join('')}<button id=resMore class=resMore>${S.resOpen?'Less ▲':'More ▾'} ${hidden.length?`(${hidden.length})`:''}</button></div>${S.resOpen&&hidden.length?`<div class=resDetail>${hidden.map(chip).join('')}</div>`:''}`;
}
function tabs(){let t=(S.state.tabs||['hab']).slice().filter(x=>x!=='diag');if(!t.includes('hab'))t.unshift('hab');if(!t.includes('console'))t.push('console');return t.slice(0,5)}
function tabLabel(t){if(t==='comms')return '🤝 '+txt('ui','ui_tab_trade');if(t==='console')return `🖥️ Log${S.unreadLogs?' 🔔 •'+S.unreadLogs:''}`;if(t==='hab')return '🏠 Hab';if(t==='surface')return '🪐 Surface';if(t==='archive')return '📚 Archive';if(t==='colony')return '👥 Colony';if(t==='ending')return '🚪 End';return txt('ui','ui_tab_'+t)}
function serverNow(){return (S.state.now_ms||0)+(Date.now()-(S.fetchedAt||Date.now()))}
function actionButton(a){let cd=(S.state.cooldowns||{})[a]||0,rem=Math.max(0,cd-serverNow());let w=rem?Math.max(0,Math.min(100,rem/((a==='open_airlock'?300:15)*1000)*100)):0;return `<button data-act="${a}" ${rem>0?'disabled':''}><span class=fill style="width:${w}%"></span><b>${icon(a)} ${txt('actions',a,'label')}</b><small>${txt('actions',a,'flavor')}</small>${rem>0?`<em>${Math.ceil(rem/1000)}s</em>`:''}</button>`}
function moduleRates(r){return Object.keys(r||{}).map(k=>icon(k)+' '+nice(k)+' '+rate(r[k],k)).join(' · ')||'none'}
function moduleButton(id,m){let owned=m.owned||0,can=m.buildable!==false;let req=m.requires_env?`<small>requires: ${cost(m.requires_env)}</small>`:'';return `<button data-build="${id}" ${can?'':'disabled'}><b>🏗️ ${m.label} ${owned?`×${owned}`:''}</b><small>${m.desc||''}</small><small>cost: ${cost(m.cost)}</small>${req}<small>rates: ${moduleRates(m.rates)}</small></button>`}
function storyList(type,ids){if(!ids||!ids.length)return '<p class=muted>Nothing recovered yet.</p>';return ids.map(id=>`<article class=entry>${txt(type,id).replace(/\n/g,'<br>')}</article>`).join('')}
function bar(label,val,max,ico='') {let p=max?Math.max(0,Math.min(100,(val/max)*100)):0;return `<div class=bar><span>${ico} ${label}</span><b>${fmt(val)} / ${fmt(max||1)}</b><i style="width:${p}%"></i></div>`}
function envPanel(s){let e=s.environment||{},r=s.resources||{};return `<div class=grid>${bar('atmosphere',r.atmosphere||0,1,'🌫️')}${bar('temperature',r.temperature||0,1,'🌡️')}${bar('biomass',r.biomass||0,1,'🌱')}${bar('breathability',e.breathability_confidence||0,100,'🫁')}</div><p class=muted>pressure ${e.pressure_mb||6.1} mb; Armstrong exposure floor ${e.armstrong_limit_mb||62} mb. Airlock ready: ${e.open_air_ready?'yes':'no'}.</p>`}
function moduleSection(title,filter,mark=''){let u=S.state.upgrades||{},keys=Object.keys(u).filter(k=>filter(k,u[k]));return keys.length?`<div class=card ${mark?`data-guide=${mark}`:''}><h2>${title}</h2>${keys.map(k=>moduleButton(k,u[k])).join('')}</div>`:''}
function recentEvents(){return `<div class=card><h2>📜 Recent log</h2>${storyList('events',(S.state.events||[]).filter(x=>x.startsWith('evt_')).slice(-6).reverse())}</div>`}
function consolePanel(){let s=S.state||{},e=s.environment||{},dev=s.device||{};let ordered=S.logs.slice().reverse();let rows=ordered.length?ordered.map((x,i)=>`<li class="${i===ordered.length-1?'latest':''}"><i>&gt;</i><span>${logFmt(x.text)}</span></li>`).join(''):'<li class=latest><i>&gt;</i><span>No console entries yet. Run operations, build modules, or return after time away.</span></li>';return `<div class=card systemCard><h2>🚀 ${actTitle(s)}</h2><p>${s.goal||''}</p><details><summary>tiny system readout</summary><p class=muted>Pressure ${e.pressure_mb||6.1} mb · Confidence ${e.breathability_confidence||0}% · Legacy ${s.legacy||0}</p><p class=muted>Build ${s.version||'?'} · care-package signature ${(dev&&dev.signature_resource)||'unknown'}</p></details></div><div class=card><h2>🖥️ Console Log</h2><p class=muted>Terminal transcript. Newest entry appears at the bottom.</p><ol class=consoleLog>${rows}</ol></div>`}
function actTitle(s){return `Act ${s.act??0} // ${(s.device&&s.device.hab_name)||'Mars Hab'}`}
function actPanel(){let s=S.state||{},e=s.environment||{},dev=s.device||{};return S.actOpen?`<div class=card id=actPanel><h2>🚀 ${actTitle(s)}</h2><p>${s.goal||''}</p><details><summary>tiny system readout</summary><p class=muted>Pressure: ${e.pressure_mb||6.1} mb · Confidence: ${e.breathability_confidence||0}% · Legacy: ${s.legacy||0}</p><p class=muted>Build ${s.version||'?'} · care-package signature ${(dev&&dev.signature_resource)||'unknown'}</p></details></div>`:''}
function stageFlash(){let s=S.state||{};return S.stageFlash?`<div class=stageFlash><h2>🚀 ${actTitle(s)}</h2><p>${s.goal||''}</p></div>`:''}
function settingsPanel(){return S.settingsOpen?`<div class=card id=settings><h2>⚙️ Settings</h2><p class=muted>Maintenance controls for this MarsHab device.</p><button id=resetProgress class=${S.resetArmed?'danger':''}>${S.resetArmed?'Confirm reset progress':'Reset progress'}</button><small class=muted>${S.resetArmed?'This clears colony progress and restarts onboarding. Tap again to confirm.':'Hidden here to avoid accidental taps during play.'}</small></div>`:''}
function render(){if(!S.state)return;res();let s=S.state;if(!tabs().includes(S.tab))S.tab='hab';$('#tabs').innerHTML=tabs().map(t=>`<button data-tab=${t} class="${S.tab===t?'on':''} ${t==='console'&&S.unreadLogs?'new':''} ${t==='console'&&S.logFlashUntil>Date.now()?'flash':''}">${tabLabel(t)}</button>`).join('');let env=s.environment||{};if(S.stageSeen!==s.act){S.stageSeen=s.act;S.stageFlash=true;setTimeout(()=>{S.stageFlash=false;render()},2600)}let html=`<div class=topTools><button id=guide class=guideBtn>🧭 Guide</button><button id=actBtn class=actBtn title="Act ${s.act}" aria-label="Act ${s.act} details">🚀 ${s.act}</button><button id=settingsBtn class=settingsBtn>⚙️</button></div>${settingsPanel()}${actPanel()}${stageFlash()}${S.lastMsg?`<p class=msg>${S.lastMsg}</p>`:''}`;
 if(S.tab==='hab')html+=`<div class=card data-guide=ops><h2>🕹️ ${txt('ui','ui_header_actions')}</h2>${(s.actions||[]).filter(a=>!['survey_crater','run_atmo_processor','seed_bioreactor','stabilize_dome'].includes(a)).map(actionButton).join('')}</div>`+moduleSection('🏠 Hab modules',(k,m)=>m.act<=2,'mods')+moduleSection('🪐 Terraforming modules',(k,m)=>m.act>=3,'mods');
 if(S.tab==='surface')html+=`<div class=card><h2>🪐 Surface Operations</h2><button class=miniHelp data-replay=surface>Replay Surface guide</button>${envPanel(s)}${(s.actions||[]).filter(a=>['survey_crater','run_atmo_processor','seed_bioreactor','stabilize_dome'].includes(a)).map(actionButton).join('')||'<p class=muted>Build industry to unlock expeditions and terraforming controls.</p>'}</div>`;
 if(S.tab==='colony')html+=`<div class=card><h2>👥 Colony</h2>${bar('population',(s.resources||{}).population||0,(s.caps||{}).population||12,'👥')}${bar('food stores',(s.resources||{}).food||0,(s.caps||{}).food||80,'🥫')}${bar('oxygen reserve',(s.resources||{}).o2||0,(s.caps||{}).o2||100,'🫁')}<p class=muted>Letters unlock as the colony stabilizes and the act advances.</p>${storyList('letters',s.letters)}</div>`;
 if(S.tab==='comms'){let p=s.passport||{visits:[]},packs=carried();html+=`<div class=card data-guide=trade><h2>🤝 ${txt('ui','ui_tab_trade')}</h2><button class=miniHelp data-replay=trade>Replay Trade guide</button><p class=muted>Passport visits: ${(p.visits||[]).length} · production bonus ${Math.round((s.passport_bonus||0)*100)}%</p><button id=pkg>📦 ${txt('ui','ui_button_copy_code')}</button><p id=code class=code></p><textarea id=redeem placeholder="paste code"></textarea><button id=redeemBtn>🎟️ ${txt('ui','ui_button_redeem_code')}</button><button id=tend>🧤 ${txt('ui','ui_button_tend')}</button></div><div class=card><h2>🎒 Carried packages</h2>${packs.length?packs.map(c=>`<button data-redeem-stored="${c}"><b>🎟️ Redeem carried package</b><small>${c}</small></button>`).join(''):'<p class=muted>No carried packages in this browser.</p>'}</div>`}
 if(S.tab==='console')html+=consolePanel();
 if(S.tab==='archive')html+=`<div class=card><h2>📚 ${txt('ui','ui_header_archive')}</h2><p class=muted>${(s.archive||[]).length} archive entries · ${(s.letters||[]).length} letters recovered</p>${storyList('archive',s.archive)}</div><div class=card><h2>✉️ ${txt('ui','ui_header_letters')}</h2>${storyList('letters',s.letters)}</div>`;
 if(S.tab==='ending')html+=`<div class=card><h2>🚪 ${txt('ui','ui_header_ending')}</h2>${envPanel(s)}${storyList('ending',s.ending)}${s.complete?'<button id=newGamePlus>🔁 Begin New Game+ // keep legacy</button>':'<p class=muted>Reach airlock-ready conditions to unlock the final sequence.</p>'}</div>`;
 $('#screen').innerHTML=html+tutorial()+surfacePrompt()+tradeTutorial()+surfaceTutorial();if(S.tab==='console')requestAnimationFrame(()=>{let l=document.querySelector('.consoleLog');if(l)l.scrollTop=l.scrollHeight});applyGuide()}
function setTutLock(on){
 if(on){if(S.tutScrollY!==null)return;S.tutScrollY=window.scrollY||0;document.body.style.top=`-${S.tutScrollY}px`;document.body.classList.add('tutLock')}
 else if(S.tutScrollY!==null){let y=S.tutScrollY;S.tutScrollY=null;document.body.classList.remove('tutLock');document.body.style.top='';window.scrollTo(0,y)}
}
function applyGuide(){
 document.querySelectorAll('.guideFocus').forEach(e=>e.classList.remove('guideFocus'));
 let tut=document.querySelector('#tutorial');if(tut){tut.classList.remove('placeTop','placeBottom')}
 if(!tut){setTutLock(false);return}
 if(!S.tutTarget){setTutLock(true);return}
 let el=document.querySelector(S.tutTarget);if(!el){setTutLock(true);return}setTutLock(false);el.classList.add('guideFocus');
 requestAnimationFrame(()=>placeAndScrollGuide(el));
}
function placeAndScrollGuide(el){
 let tut=document.querySelector('#tutorial'),card=document.querySelector('.tutCard');if(!tut||!card||!el)return;
 let vh=window.innerHeight||document.documentElement.clientHeight||640,margin=22;
 let nav=document.querySelector('nav'),nr=nav?nav.getBoundingClientRect():{top:vh};
 let usableTop=margin,usableBottom=Math.min(vh-margin,nr.top-margin);
 let er=el.getBoundingClientRect(),targetMid=(er.top+er.bottom)/2;
 let spaceAbove=Math.max(0,er.top-usableTop-margin),spaceBelow=Math.max(0,usableBottom-er.bottom-margin);
 tut.classList.remove('placeTop','placeBottom');
 if(spaceAbove>=spaceBelow)tut.classList.add('placeTop');else tut.classList.add('placeBottom');
 requestAnimationFrame(()=>{
  let cr=card.getBoundingClientRect();
  let topGap=usableTop,bottomGap=usableBottom;
  if(tut.classList.contains('placeTop'))topGap=Math.min(bottomGap,cr.bottom+margin);else bottomGap=Math.max(topGap,cr.top-margin);
  if(bottomGap-topGap<80){topGap=usableTop;bottomGap=usableBottom}
  let er2=el.getBoundingClientRect(),space=bottomGap-topGap,actual=(er2.top+er2.bottom)/2,desired=(topGap+bottomGap)/2;
  if(er2.height>space){
   if(tut.classList.contains('placeTop')){actual=er2.top;desired=topGap}else{actual=er2.bottom;desired=bottomGap}
  }
  let maxY=Math.max(0,document.documentElement.scrollHeight-vh);
  let next=Math.max(0,Math.min(maxY,window.scrollY+actual-desired));
  window.scrollTo({top:next,behavior:'auto'});
  requestAnimationFrame(()=>setTutLock(true));
 });
}
async function sync(){let min=new Promise(r=>setTimeout(r,1200));let p=api('/api/sync',{body:{now:Date.now()}});let [st]=await Promise.all([p,min]);S.state=st;S.fetchedAt=Date.now();await Promise.all(TYPES.map(loadContent));$('#load').classList.add('hide');if(!S.logSeeded){(st.events||[]).slice(-8).reverse().forEach(id=>log(line(id),true));S.logSeeded=true}if(st.away_report&&st.away_report.elapsed_ms){let parts=(st.away_report.away||[]).map(id=>txt('away',id));(st.away_report.beats||[]).forEach(id=>log(txt('beats',id)));log(parts.length?parts.join(' / '):('while away: '+Object.entries(st.away_report.gains||{}).map(([k,v])=>nice(k)+' '+fmt(v)).join(', ')))}render()}
async function refreshDiag(){S.diag=await api('/api/diagnostics')}
document.body.onclick=async e=>{let rc=e.target.closest('.r');if(rc&&rc.closest('#res')&&!S.resOpen){rc.classList.add('showName');clearTimeout(rc._t);rc._t=setTimeout(()=>rc.classList.remove('showName'),1300);if(tutStep()===2){setTimeout(()=>{if(tutStep()===2&&maybeTut('resource',rc.dataset.res))render()},900)}else if(maybeTut('resource',rc.dataset.res))render();return}let b=e.target.closest('button');if(!b)return;if(b.id==='resMore'){S.resOpen=!S.resOpen;render();return}if(b.id==='settingsBtn'){S.settingsOpen=!S.settingsOpen;S.actOpen=false;S.resetArmed=false;render();return}if(b.id==='actBtn'){S.actOpen=!S.actOpen;S.settingsOpen=false;render();return}if(b.id==='resetProgress'){if(!S.resetArmed){S.resetArmed=true;render();return}let st=await api('/api/reset',{body:{now:Date.now()}});S.state=st;S.fetchedAt=Date.now();S.lastMsg='progress reset';S.resetArmed=false;S.settingsOpen=false;S.tutDone=false;S.tutReview=false;S.tutStep=0;S.logSeeded=false;S.logs=[];S.unreadLogs=0;try{['marsHabTutorialDone','marsHabTutorialSeen','marsHabTutorialStep','marsHabSurfaceTutorialDone','marsHabTradeTutorialDone','marsHabPackages'].forEach(k=>localStorage.removeItem(k))}catch(e){}render();return}if(b.dataset.replay==='surface'){S.surfaceTutDone=false;try{localStorage.removeItem('marsHabSurfaceTutorialDone')}catch(e){}S.tab='surface';render();return}if(b.dataset.replay==='trade'){S.tradeTutDone=false;try{localStorage.removeItem('marsHabTradeTutorialDone')}catch(e){}S.tab='comms';render();return}if(b.dataset.surfaceTut){S.surfaceTutDone=true;try{localStorage.setItem('marsHabSurfaceTutorialDone','1')}catch(e){}render();return}if(b.dataset.tradeTut){S.tradeTutDone=true;try{localStorage.setItem('marsHabTradeTutorialDone','1')}catch(e){}render();return}if(b.dataset.tut){if(b.dataset.tut==='skip')closeTut(true);else if(b.dataset.tut==='back')backTut();else{let n=tutStep()+1;if(n>9)closeTut(true);else{setTutStep(n);render()}}return}if(b.id==='guide'){openTut();return}if(b.dataset.tab){S.tab=b.dataset.tab;if(S.tab==='console'){S.unreadLogs=0;S.logFlashUntil=0}maybeTut('tab',S.tab);render();return}if(b.dataset.act){let st=await api('/api/action',{body:{action_id:b.dataset.act}});S.state=st;S.fetchedAt=Date.now();S.lastMsg=st.result&&st.result.ok?'action complete':((st.result&&st.result.error)||'action failed');if(st.result&&st.result.event)log(line(st.result.event));if(st.result&&st.result.beats)(st.result.beats||[]).forEach(id=>log(txt('beats',id)));if(st.result&&st.result.ok)maybeTut('action',b.dataset.act);render();return}if(b.dataset.build){let st=await api('/api/action',{body:{action_id:'build',module_id:b.dataset.build}});S.state=st;S.fetchedAt=Date.now();S.lastMsg=st.result&&st.result.ok?'module built':((st.result&&st.result.error)||'build failed');if(st.result&&st.result.beats)(st.result.beats||[]).forEach(id=>log(txt('beats',id)));if(st.result&&st.result.ok)maybeTut('build',b.dataset.build);render();return}if(b.dataset.redeemStored){let code=b.dataset.redeemStored;S.state=await api('/api/trade/redeem',{body:{code}});S.fetchedAt=Date.now();if(S.state.result&&S.state.result.ok)forgetCode(code);S.lastMsg=(S.state.result&&S.state.result.ok)?'carried package redeemed':((S.state.result&&S.state.result.error)||'redeem failed');render();return}if(b.id==='pkg'){let r=await api('/api/trade/package');$('#code').textContent=r.code;rememberCode(r.code);maybeTut('package');render();return}if(b.id==='redeemBtn'){let code=$('#redeem').value;S.state=await api('/api/trade/redeem',{body:{code}});S.fetchedAt=Date.now();if(S.state.result&&S.state.result.ok)forgetCode(code);S.lastMsg=(S.state.result&&S.state.result.ok)?'package redeemed':((S.state.result&&S.state.result.error)||'redeem failed');render();return}if(b.id==='tend'){S.state=await api('/api/tend',{body:{visitor:'guest'}});S.fetchedAt=Date.now();S.lastMsg='host tended';render();return}if(b.id==='newGamePlus'){S.state=await api('/api/action',{body:{action_id:'new_game_plus'}});S.fetchedAt=Date.now();S.lastMsg=(S.state.result&&S.state.result.ok)?'new game+ started':((S.state.result&&S.state.result.error)||'reset failed');S.tab='hab';render();return}};
setInterval(async()=>{if(!S.state)return;S.state=await api('/api/state');S.fetchedAt=Date.now();render()},2000);
loadTut();
sync();
